{
  "name": "studyflow-android",
  "version": "3.22.0",
  "private": true,
  "displayName": "StudyFlow",
  "description": "Pomodoro / Heatmap study tracker — Android build",
  "author": "nine-to-five-studio",
  "main": "index.js",
  "scripts": {
    "cordova": "cordova",
    "prepare": "node scripts/sync-web-assets.js",
    "plugins:restore": "cordova plugin add cordova-plugin-android-permissions --no-interactive",
    "platform:add": "node scripts/sync-web-assets.js && cordova platform add android --no-interactive",
    "build:apk": "node scripts/sync-web-assets.js && npm run plugins:restore && cordova build android --debug --no-interactive"
  },
  "devDependencies": {
    "cordova": "^12.0.0",
    "cordova-android": "^13.0.0"
  },
  "cordova": {
    "platforms": [
      "android"
    ],
    "plugins": {
      "cordova-plugin-android-permissions": {}
    }
  }
}